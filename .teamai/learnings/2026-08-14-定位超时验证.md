# 定位超时验证结论 (worklog 任务#11)

- TIMEOUT_AXIS_MOVE 声明未用 = 形同虚设; FB 必须有 Timeout/Expired 接口
- 电平直驱禁用于上位机命令; 必须 FB_CmdHandshake 锁存
- TONR 是保持型定时器: 重复计时场景必须 R 复位, 否则 ET 跨启动累积导致提前误报
- 报警必须接入锁存闭环(置位→锁存→ErrorID→RunOK), 只置位不锁存 = 形同虚设
