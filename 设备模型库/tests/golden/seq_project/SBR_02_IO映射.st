(* ════════════════════════════════════════════════════════════════════
   SBR_02 IO 映射 - seq_project
   急停不滤波(filter_mode=none), 内置滤波(builtin), 额外消抖(debounce)
   ════════════════════════════════════════════════════════════════════ *)(* 急停/安全不滤波: 急停输入(常闭,FALSE=触发,不滤波) *)
(* ── 物理输入映射 ── *)in_EStop := X0;   (* 急停输入(常闭,FALSE=触发,不滤波) *)in_SafetyGate := X1;   (* 安全门(常闭) *)in_GripperMotor_Err := X10;   (* 夹爪步进故障 *)in_LeftBlock_Err := X11;   (* 左阻挡步进故障 *)in_RightBlock_Err := X12;   (* 右阻挡步进故障 *)in_Roller_Err := X13;   (* 辊筒控制卡故障 *)in_Grip_LimitPos := X20;   (* 夹爪正向限位(常闭) *)in_Grip_LimitNeg := X21;   (* 夹爪反向限位(常闭) *)in_LBlock_LimitPos := X22;   (* 左阻挡正向限位(常闭) *)in_LBlock_LimitNeg := X23;   (* 左阻挡反向限位(常闭) *)in_RBlock_LimitPos := X24;   (* 右阻挡正向限位(常闭) *)in_RBlock_LimitNeg := X25;   (* 右阻挡反向限位(常闭) *)in_Roller_Material := X26;   (* 辊筒物料检测 *)in_Auto := X14;   (* 模式选择-自动位(物理开关) *)in_Manual := X15;   (* 模式选择-手动位(物理开关) *)
(* ── 物理输出映射 ── *)Y0 := out_Light_Red;   (* 红灯(报警) *)Y1 := out_Light_Green;   (* 绿灯(自动) *)Y2 := out_Light_Yellow;   (* 黄灯(手动) *)Y3 := out_Buzz;   (* 蜂鸣器 *)Y10 := out_Gripper_Reset;   (* 夹爪复位 *)Y11 := out_Gripper_Stop;   (* 夹爪停止 *)Y12 := out_Gripper_Path1;   (* 夹爪路径1(夹紧) *)Y13 := out_Gripper_Path2;   (* 夹爪路径2(松开) *)Y14 := out_LBlock_Reset;   (* 左阻挡复位 *)Y15 := out_LBlock_Stop;   (* 左阻挡停止 *)Y16 := out_LBlock_Path1;   (* 左阻挡路径1(上升) *)Y17 := out_LBlock_Path2;   (* 左阻挡路径2(下降) *)Y20 := out_RBlock_Reset;   (* 右阻挡复位 *)Y21 := out_RBlock_Stop;   (* 右阻挡停止 *)Y22 := out_RBlock_Path1;   (* 右阻挡路径1(上升) *)Y23 := out_RBlock_Path2;   (* 右阻挡路径2(下降) *)Y30 := out_CC_Run;   (* 控制卡运行使能 *)Y31 := out_CC_Dir;   (* 控制卡方向 *)Y32 := out_CC_SpeedA;   (* 控制卡速度档A *)Y33 := out_CC_SpeedB;   (* 控制卡速度档B *)Y40 := out_EStop_Reset;   (* 急停继电器复位 *)