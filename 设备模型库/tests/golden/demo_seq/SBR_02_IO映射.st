(* ════════════════════════════════════════════════════════════════════
   SBR_02 IO 映射 - demo_seq
   急停不滤波(filter_mode=none), 内置滤波(builtin), 额外消抖(debounce)
   ════════════════════════════════════════════════════════════════════ *)(* 急停/安全不滤波: 急停(常闭,不滤波) *)
(* ── 物理输入映射 ── *)in_EStop := X0;   (* 急停(常闭,不滤波) *)in_Auto := X1;   (* 自动位 *)in_Manual := X2;   (* 手动位 *)in_Grip_LimitPos := X10;   (* 夹爪正限位(常闭) *)in_Grip_LimitNeg := X11;   (* 夹爪负限位(常闭) *)
(* ── 物理输出映射 ── *)Y0 := out_Light_Red;   (* 红灯(报警) *)Y1 := out_Light_Green;   (* 绿灯(自动) *)Y2 := out_Light_Yellow;   (* 黄灯(手动) *)Y10 := out_Gripper_Path1;   (* 夹爪路径1(夹紧) *)Y11 := out_Gripper_Path2;   (* 夹爪路径2(松开) *)